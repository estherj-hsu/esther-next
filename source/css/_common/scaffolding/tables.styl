table {
  margin: 20px 0;
  width: $table-width;
  border-collapse: collapse;
  border-spacing: 0;
  border: 1px solid $table-border-color;
  font-size: $table-font-size;
  table-layout: fixed;
  word-wrap: break-all;
}
table>tbody>tr {
  &:nth-of-type(odd) { background-color: $table-row-odd-bg-color; }
  &:hover { background-color: $table-row-hover-bg-color; }
}

caption, th, td {
  padding: $table-cell-padding;
  text-align: $table-content-alignment;
  vertical-align: $table-content-vertical;
  font-weight: normal;
}

th, td {
  border-bottom: 3px solid $table-cell-border-bottom-color;
  border-right: 1px solid $table-cell-border-right-color;
}
h2#Education + table th:first-of-type,
h2#Education + table td:first-of-type {
  width: 80px;
  text-align: center;
}
h2#Experience + table th:first-of-type,
h2#Experience + table td:first-of-type {
  width: 120px;
  text-align: center;
}

th {
  padding-bottom: 10px;
  font-weight: $table-th-font-weight;
}

td { border-bottom-width: 1px; }